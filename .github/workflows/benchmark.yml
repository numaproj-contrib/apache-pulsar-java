name: Benchmark

on:
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      measurement_duration:
        description: 'Measurement duration in seconds (after warmup)'
        required: false
        default: '120'
      prefill_duration:
        description: 'How long to run the producer before starting the consumer (seconds)'
        required: false
        default: '300'

permissions:
  contents: write

jobs:
  benchmark:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ── Build the application image ──────────────────────────────────
      - name: Set up JDK 23
        uses: actions/setup-java@v4
        with:
          java-version: '23'
          distribution: 'temurin'
          cache: maven

      - name: Build image with Jib
        run: mvn clean package -DskipTests

      - name: Resolve image tag
        id: image
        run: |
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "tag=apache-pulsar-java:v${VERSION}" >> "$GITHUB_OUTPUT"

      # ── k3d cluster ──────────────────────────────────────────────────
      - name: Install k3d
        run: curl -sfL https://raw.githubusercontent.com/rancher/k3d/main/install.sh | bash

      - name: Create k3d cluster
        run: |
          k3d cluster create benchmark
          kubectl cluster-info

      - name: Install metrics-server
        run: |
          kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
          kubectl -n kube-system patch deployment metrics-server --type=json \
            -p '[{"op":"add","path":"/spec/template/spec/containers/0/args/-","value":"--kubelet-insecure-tls"}]'

      - name: Import application image into k3d
        run: k3d image import ${{ steps.image.outputs.tag }} -c benchmark

      # ── Install Numaflow ─────────────────────────────────────────────
      - name: Install Numaflow controller
        run: |
          kubectl create ns numaflow-system
          kubectl apply -n numaflow-system \
            -f https://raw.githubusercontent.com/numaproj/numaflow/stable/config/install.yaml
          kubectl -n numaflow-system rollout status deployment/numaflow-controller --timeout=120s

      - name: Install ISB service (JetStream)
        run: |
          kubectl apply \
            -f https://raw.githubusercontent.com/numaproj/numaflow/stable/examples/0-isbsvc-jetstream.yaml
          echo "Waiting for ISB StatefulSet to be ready..."
          sleep 10
          kubectl wait --for=condition=Ready pod -l app.kubernetes.io/part-of=numaflow \
            -l app.kubernetes.io/component=isbsvc --timeout=180s || \
            kubectl get pods -A

      # ── Deploy Pulsar inside the cluster ─────────────────────────────
      - name: Deploy Pulsar standalone
        run: |
          kubectl apply -f .github/benchmark/pulsar.yaml
          echo "Waiting for Pulsar to be ready..."
          kubectl wait --for=condition=Ready pod/pulsar --timeout=180s

      - name: Create benchmark topic
        run: |
          kubectl exec pulsar -- bin/pulsar-admin topics create \
            persistent://public/default/benchmark-topic

      # ── Deploy ConfigMaps ────────────────────────────────────────────
      - name: Apply ConfigMaps
        run: |
          kubectl apply -f .github/benchmark/consumer-config.yaml
          kubectl apply -f .github/benchmark/producer-config.yaml

      # ── Substitute image tag in manifests and deploy ─────────────────
      - name: Deploy producer pipeline (load generator)
        run: |
          sed "s|__IMAGE_TAG__|${{ steps.image.outputs.tag }}|g" \
            .github/benchmark/producer-pipeline.yaml | kubectl apply -f -
          echo "Pre-filling topic for ${{ inputs.prefill_duration || '300' }}s..."
          sleep 10
          kubectl wait --for=condition=Ready pod -l numaflow.numaproj.io/pipeline-name=producer-pipeline \
            --timeout=120s || kubectl get pods
          sleep ${{ inputs.prefill_duration || '300' }}

      - name: Check topic stats
        run: |
          kubectl exec pulsar -- bin/pulsar-admin topics stats \
            persistent://public/default/benchmark-topic \
            | grep -E '"msgInCounter"|"msgOutCounter"|"storageSize"|"msgRateIn"'

      - name: Deploy consumer MonoVertex
        run: |
          sed "s|__IMAGE_TAG__|${{ steps.image.outputs.tag }}|g" \
            .github/benchmark/consumer-monovertex.yaml | kubectl apply -f -

      # ── Capture metrics ──────────────────────────────────────────────
      - name: Run benchmark and capture metrics
        run: .github/benchmark/capture-metrics.sh ${{ inputs.measurement_duration || '120' }}

      # ── Report results ───────────────────────────────────────────────
      - name: Store benchmark result
        uses: benchmark-action/github-action-benchmark@v1
        with:
          tool: customBiggerIsBetter
          output-file-path: benchmark-results.json
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: true
          alert-threshold: '200%'
          comment-on-alert: true
          fail-on-alert: false
          benchmark-data-dir-path: dev/bench

      # ── Debug / cleanup ──────────────────────────────────────────────
      - name: Dump pod status on failure
        if: failure()
        run: |
          echo "=== Pod status ==="
          kubectl get pods -A
          echo ""
          echo "=== Consumer MonoVertex logs ==="
          kubectl logs -l numaflow.numaproj.io/mono-vertex-name=consumer-mvtx --all-containers --tail=50 || true
          echo ""
          echo "=== Producer pipeline logs ==="
          kubectl logs -l numaflow.numaproj.io/pipeline-name=producer-pipeline --all-containers --tail=50 || true
          echo ""
          echo "=== Pulsar logs ==="
          kubectl logs pulsar --tail=30 || true

      - name: Cleanup
        if: always()
        run: k3d cluster delete benchmark
